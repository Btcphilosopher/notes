

Used 2 tools
Let me check Icarus's SVA (assert property, bind) support before designing the formal assertions file, since tool support varies a lot here.


Ran 3 commands (2 failed)
Confirmed: Icarus 12.0 supports immediate assertions (assert (expr) else ...) but its parser rejects property/assert property/bind entirely (concurrent SVA). I'll design formal/sha256_properties.sv as a checker module using immediate assertions with manual edge-tracking instead of bind+temporal properties — same verification intent, portable to this toolchain.

Created
sha256_properties.sv
+92
-0
Now let's wire this checker into tb_miner_top.sv and confirm it doesn't spuriously fire under the existing passing scenarios:


Edited 2 files, ran 2 commands (1 failed)
+22
-14
Clean pass with zero assertion failures across all 4 scenarios (including two resets) — the checks hold under real operation. Let me also prove they're not vacuous by deliberately injecting one bug into a scratch copy and confirming the checker actually catches it.


Ran 3 commands
Confirmed non-vacuous — the checker caught the injected bug immediately. Let me commit a proper, lightweight, reproducible version of this self-test to the repo (a small local stub module, not polluting rtl/) rather than leaving it as an ad-hoc /tmp experiment.


Created tb_formal_checker_selftest.sv, ran a command, used 2 tools
+74
-0
Now the verification automation — simulate.py with a full testbench manifest, and verify.py tying vectors→simulate together:


Created 4 files, ran 2 commands, edited a file
+279
-1
Three real (benign) width-expansion lint warnings — no functional bugs, but easy to fix cleanly. Let me fix all three.


Read 2 files, edited 2 files
+2
-2
Now telemetry.sv:


Read and edited telemetry.sv, ran a command
+4
-2
Clean lint pass. Let me quickly re-verify the fixed modules still simulate correctly (these were pure width-explicit refactors, but let's confirm):

Ran
a command
Now let's run the complete automated verification pipeline (--json for a saved record) — this will take a few minutes due to the deep-pipeline depths:


Ran 2 commands
Not a bug — genuinely takes ~36s (same "deep unregistered combinational chain" Icarus performance characteristic as the low pipeline depths), just over my 30s manifest timeout. Let me bump it.


Edited simulate.py, ran a command
+7
-1
All 25/25 pass — the automated pipeline is fully working. Now let me address the "10/100/1000/10000/100000+" vector scaling from section 28. I'll scale the RTL-vs-Python checks substantially (limited by simulator wall-clock) and add a dedicated large-scale (100,000+) pure-Python self-consistency check, clearly labeled as a separate category.


Edited 2 files, ran a command
+56
-12
Two failures need investigation. Let me check tb_target_compare first — it looks like my own edge-case vector generation is inconsistent about byte-order convention (same class of self-inflicted bug as before), not an RTL issue. Let me verify by inspection, then investigate the more concerning tb_sha256_compressor hang.

Ran
a command
Both explained — not RTL bugs. tb_sha256_compressor's hang is just its internal 2ms simulated-time safety timeout being too small now that the vector count is ~10x larger (3953 × ~660ns/case ≈ 27ms needed, but only 2ms was budgeted). Let me fix both.


Read and edited tb_sha256_compressor.sv
+5
-2
Now let's fix the target_compare edge-case vector generation to use consistent byte-order semantics:

Edited
generate_vectors.py
+22
-4
Now let's regenerate vectors and re-run the full verification pipeline to confirm 25/25:

Ran
a command


